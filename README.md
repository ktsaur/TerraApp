🌍 TerraApp — мобильное приложение для Android, предназначенное для поиска достопримечательностей и просмотра прогноза погоды в выбранных городах.

Приложение позволяет пользователям авторизоваться, просматривать погоду, список достопримечательностей с деталями, добавлять их в избранное и управлять профилем. 


⚙️ Функционал
Авторизация и регистрация: Пользователь может зарегистрироваться (username, email, password) или войти в аккаунт. Пароли хешируются с помощью BCrypt. После входа доступны фичи.

Просмотр погоды: Ввод города, отображение текущей температуры, описания погоды, прогноза на ближайшие два дня. Поддержка клика на прогноз для детальной информации (ощущаемая температура, мин/макс, описание).

Просмотр достопримечательностей: Для введённого города отображается список достопримечательностей с названием, рейтингом, адресом, изображением. Клик на карточку открывает детали (описание, категория, координаты).

Избранное: Добавление/удаление достопримечательностей в избранное. Отдельный экран для просмотра избранного.

Профиль: Отображение username и email. Кнопки "Выйти из аккаунта" и "Удалить аккаунт".

Firebase интеграция: Автоматический сбор аналитики (открытие экранов), крашей и производительности.


🏗️ Архитектура

Presentation Layer: Jetpack Compose для UI, MVI-архитектура (своя реализация с ViewModel, State, Event, Effect). Навигация через Jetpack Navigation Compose.

Domain Layer: UseCase для бизнес-логики (например, GetForecastByCityNameUseCase), модели данных, репозитории (интерфейсы).

Data Layer: Реализация репозиториев с Retrofit (для API), Room (для локальной БД), OkHttp (для сети).

DI: Hilt для инъекции зависимостей.

Coroutine: Для асинхронных операций (withContext(IO)).

Мультимодульность: Разделение на app, core (base, data, domain), feature (auth, attractions, favourites, profile).


🔧 Технический стек

Язык: Kotlin 

UI: Jetpack Compose, Material3, Coil 2.5.0 для изображений

Архитектура: MVI (своя), Clean Architecture

Навигация: Navigation Compose 2.8.9

DI: Hilt 2.48

Сеть: Retrofit 2.11.0, OkHttp 4.12.0, Gson Converter 2.3.0, Kotlinx Serialization 1.6.0

БД: Room 2.6.1 (с KSP 1.9.22-1.0.17)

Хранение: SharedPreferences (для AuthManager), BCrypt 0.4 для хеширования паролей

Тесты: JUnit 4.13.2, Mockito-Kotlin 5.2.1, Kotlinx-Coroutines-Test 1.8.0, Kotlin-Test 1.9.22

Firebase: Analytics, Crashlytics, Performance (BoM 32.5.0)


Используемые API в проекте TerraApp

В приложении TerraApp данные получаются из двух источников:

1) OpenWeatherMap API (публичное API для погоды)
2) 
Назначение: Получение текущей погоды и прогноза на ближайшие дни по названию города.

Используемые эндпоинты:

GET /data/2.5/weather — текущая температура, описание погоды, иконка.

GET /data/2.5/forecast — почасовой/ежедневный прогноз (из него берётся прогноз на завтра и послезавтра на 12:00).

Формат ответа: JSON.

Интеграция: Через Retrofit с базовым URL https://api.openweathermap.org/data/2.5/. API-ключ хранится в BuildConfig.


2)Собственное mock-API на базе Mockoon (локальное эмулированное API)

<img width="1470" height="922" alt="Image" src="https://github.com/user-attachments/assets/8ea80b17-9064-44d8-8014-49c153ecd4ce" />

Назначение: Получение списка достопримечательностей по названию города.

Тип: Локальный mock-сервер, запущенный на эмуляторе по адресу http://10.0.2.2:3000/

Эндпоинт: GET /attractions?city={city} 

Формат ответа: JSON с полями status, city, attractions (массив объектов Attraction), total.

Модель данных: Attraction (id, name, description, rating, address, imageUrl, category, latitude, longitude).

Интеграция: Через Retrofit с базовым URL из BuildConfig (MOCKOON_API_URL). Сервис AttractionsApiService.

В Mockoon можно удобно просматривать все совершенные запросы, менять данные

📦  Модули

app: Основной модуль приложения. Содержит Application-класс (TerraApp с инициализацией Firebase), навигацию (NavHost с маршрутами), главные экраны (MainActivity). Отвечает за сборку всего приложения, интеграцию модулей и запуск.
core: Базовые модули для общих компонентов.

core:base: Базовые утилиты, расширения (AnalyticsHelper, AuthManager)

core:data: Репозитории (ForecastRepositoryImpl, AttractionsRepositoryImpl, UserRepositoryImpl), сетевые клиенты (Retrofit с OpenWeather API), БД (Room: AppDatabase, UserDao). 
Отвечает за источники данных (API, локальная БД).

core:domain: Модели (Forecast, Attraction, User), UseCase (GetForecastByCityNameUseCase, GetAttractionsByCityNameUseCase, AddFavouriteUseCase, DeleteUserUseCase и т.д.), интерфейсы репозиториев. Отвечает за бизнес-логику

feature: Фич-модули, каждый для отдельной фичи.

feature:auth: Экраны авторизации/регистрации (AuthScreen, RegistrScreen), ViewModel (AuthorizationViewModel, RegistrViewModel), State/Event/Effect. Отвечает за вход/регистрацию.

feature:attractions: Экраны достопримечательностей и погоды (AttractionsScreen, AttractionDetailsScreen), ViewModel (MainScreenViewModel), State/Event/Effect. Отвечает за поиск города, погоду, список/детали достопримечательностей.

feature:favourites: Экран избранного (FavouriteScreen), ViewModel (FavouriteScreenViewModel), State/Event/Effect. Отвечает за управление избранным.

feature:profile: Экран профиля (ProfileScreen), ViewModel (ProfileViewModel), State/Event/Effect. Отвечает за отображение профиля, выход/удаление аккаунта.


🧪 Тесты для UseCase

Юнит-тесты написаны для следующих UseCase:

DeleteUserUseCaseTest
GetAttractionsByCityNameUseCaseTest
GetForecastByCityNameUseCaseTest
GetUserUseCaseTest
GetWeatherByCityNameUseCaseTest.

🔥 8. Работа Firebase

Firebase интегрирован для аналитики, краш-репортинга и мониторинга производительности:

Analytics: Автоматически собирает события открытия экранов (в init ViewModel: AnalyticsHelper.logScreenView(screenName, screenClass))


<img width="1470" height="837" alt="Image" src="https://github.com/user-attachments/assets/bbf794f8-1187-4674-9f97-8aadda9e2d75" />


Crashlytics: Автоматически отправляет краши (инициализация в TerraApp). В консоли видны отчёты о крашах с стек-трейсом.


<img width="1470" height="837" alt="Image" src="https://github.com/user-attachments/assets/13ee0f8a-7b64-42ad-89e4-f03a06acb216" />


Performance Monitoring:сетевые запросы (Retrofit), рендеринг экранов


<img width="1470" height="837" alt="Image" src="https://github.com/user-attachments/assets/acd9debd-baf3-4272-8a08-07c78c93242b" />


<img width="1470" height="837" alt="Image" src="https://github.com/user-attachments/assets/7d634fc4-5d74-45cf-92b6-6877c45140ba" />


<img width="1470" height="837" alt="Image" src="https://github.com/user-attachments/assets/84f0fa03-72da-49d8-ab31-9e8cd5bf28c7" />


Ссылка на гугл диск с скринкастом приложения: https://drive.google.com/drive/folders/1FZJ5qQCoQHN0bmL5ZqoYHyJNdTrh9FYn?usp=sharing





